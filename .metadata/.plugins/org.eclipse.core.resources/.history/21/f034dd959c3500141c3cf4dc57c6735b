package twgitter.gitter;

import twgitter.get.GetHTTP;
import twgitter.gitter.config.Config;

import com.google.gson.Gson;

public class Streaming {
	public static void main(String[] args) throws Exception {
		/*
		String roomName = "S--Minecraft/twgitter";

		String streamRoomId = GetRooms.roomNameToId(roomName);
		String URI = "https://stream.gitter.im/v1/rooms/" + streamRoomId + "/chatMessages";
		String[][] Header = { {"Authorization", "Bearer " + Config.token}, {"Accept", "application/json"} };
		String CharCode = "UTF-8";

		//curl -k -i -H "Accept: application/json" -H "Authorization: Bearer f942da113703b8fb345a9277945098757265255e" "https://stream.gitter.im/v1/rooms/54085e0f163965c9bc202908/chatMessages

		StringBuilder streamStringBuilder = GetHTTP.AccessHTTP(URI,Header,CharCode);

		Gson gson = new Gson();
		ChatString chatString = gson.fromJson(streamStringBuilder.toString(),ChatString.class);

		System.out.println("[" + chatString.getFromUser().getDisplayName() + "(" + chatString.getFromUser().getUsername()  + ")]" + chatString.getText());
		*/
	}

}
